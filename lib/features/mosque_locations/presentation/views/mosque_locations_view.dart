import 'package:flutter/material.dart';

class MosqueLocationsView extends StatelessWidget {
  const MosqueLocationsView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Mosque Locations View'),
    );
  }
}
