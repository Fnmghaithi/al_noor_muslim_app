import 'package:flutter/material.dart';

class HolyQuranView extends StatelessWidget {
  const HolyQuranView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Holy Quran View'),
    );
  }
}
